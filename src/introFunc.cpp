/*

#include "main_header.h"

std :: string intro()
{
    std :: string UI{};
    std :: cout << "Are you a new or existing user? \n";
    std :: cout << "Enter new for a new user and CURR for an existing user \n";
    std :: cin >> UI;  //lowercase
    if(UI == "new")
    {
        newUser();
    }
    else if(UI == "curr")
    {
        std :: cout << "existingUser()";
    }
    else
        intro();

}
*/
